<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="movie">

	<select id="selectMovies" parameterType="String" resultType="MovieVO">
		SELECT MOVIE_NAME
		  FROM MOVIE
		 WHERE MOVIE_ID = #{movieId}
	</select>

	<select id="selectMovie" parameterType="string" resultType="MovieVO">
		SELECT MOVIE_NAME, MOVIE_DIRECTOR, MOVIE_GENRE
		FROM MOVIE
		WHERE MOVIE_ID = #{movieId}
	</select>


</mapper>